# CLI 命令行选项支持实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 `shun-code` 增加完整的命令行参数支持（版本、帮助、日志级别、交互式、会话查看、单命令执行）。

**Architecture:** 引入 `clap` derive 宏解析 CLI 参数；新增轻量级 `log_debug!` 宏替代硬编码的 `eprintln!`；`main.rs` 按 `-s > -c > 默认交互式` 的优先级调度；`SessionManager` 新增会话查找与打印辅助方法。

**Tech Stack:** Rust, `clap` (derive feature), `serde_json`

---

## File Map

- **Create:** `src/cli.rs` — `clap` 的 `Args` 结构体定义
- **Create:** `src/log.rs` — 全局 `DEBUG_ENABLED` 原子开关 + `log_debug!` 宏
- **Modify:** `Cargo.toml` — 添加 `clap` 依赖
- **Modify:** `src/main.rs` — 解析 CLI、调度分支、提取 `run_interactive`
- **Modify:** `src/session/session.rs` — 新增 `find_session`、`print_session_messages` 等辅助方法
- **Modify:** `src/agent/agent.rs` — 替换 `eprintln!("[debug] ...")` 为 `log_debug!(...)`
- **Modify:** `src/tools/mod.rs` — 替换 debug 日志
- **Modify:** `src/provider/client/openapi_client.rs` — 替换 debug 日志
- **Modify:** `src/provider/client/anthropic_client.rs` — 替换 debug 日志

---

### Task 1: 添加 `clap` 依赖并创建 `src/cli.rs`

**Files:**
- Modify: `Cargo.toml`
- Create: `src/cli.rs`

- [ ] **Step 1: 添加 `clap` 到 `Cargo.toml`**

在 `[dependencies]` 段末尾插入：

```toml
clap = { version = "4", features = ["derive"] }
```

- [ ] **Step 2: 创建 `src/cli.rs`**

```rust
use clap::Parser;

#[derive(Parser, Debug)]
#[command(name = "shun-code", version = env!("CARGO_PKG_VERSION"))]
pub struct Args {
    /// Enable debug logging (debug|info, default: info)
    #[arg(short = 'l', long = "log", value_name = "LEVEL", default_value = "info")]
    pub log_level: String,

    /// Enter interactive REPL mode
    #[arg(short = 'i', long = "interactive")]
    pub interactive: bool,

    /// Print session information and exit
    #[arg(short = 's', long = "session", value_name = "SESSION", num_args = 0..=1)]
    pub session: Option<Option<String>>,

    /// Execute a single command and exit
    #[arg(short = 'c', long = "command", value_name = "MESSAGE")]
    pub command: Option<String>,
}
```

- [ ] **Step 3: 验证编译通过**

Run: `cargo check`
Expected: 成功（此时 `src/cli.rs` 还未被 `main.rs` 引用，但会被编译进 crate）

- [ ] **Step 4: Commit**

```bash
git add Cargo.toml src/cli.rs
git commit -m "deps: add clap and define CLI Args struct"
```

---

### Task 2: 创建 `src/log.rs` 与 `log_debug!` 宏

**Files:**
- Create: `src/log.rs`

- [ ] **Step 1: 创建 `src/log.rs`**

```rust
use std::sync::atomic::{AtomicBool, Ordering};

static DEBUG_ENABLED: AtomicBool = AtomicBool::new(false);

pub fn set_debug(enabled: bool) {
    DEBUG_ENABLED.store(enabled, Ordering::Relaxed);
}

pub fn is_debug() -> bool {
    DEBUG_ENABLED.load(Ordering::Relaxed)
}

#[macro_export]
macro_rules! log_debug {
    ($($arg:tt)*) => {
        if $crate::log::is_debug() {
            eprintln!("[debug] {}", format!($($arg)*));
        }
    };
}
```

- [ ] **Step 2: 在 `src/log.rs` 中写单元测试**

在文件末尾追加：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use std::sync::atomic::Ordering;

    #[test]
    fn test_debug_toggle() {
        set_debug(true);
        assert!(is_debug());
        set_debug(false);
        assert!(!is_debug());
    }
}
```

- [ ] **Step 3: 运行测试**

Run: `cargo test log::tests`
Expected: `test_debug_toggle` PASS

- [ ] **Step 4: Commit**

```bash
git add src/log.rs
git commit -m "feat: add lightweight debug log toggle and log_debug macro"
```

---

### Task 3: 替换现有代码中的 `eprintln!("[debug] ...")` 为 `log_debug!(...)`

**Files:**
- Modify: `src/agent/agent.rs`
- Modify: `src/tools/mod.rs`
- Modify: `src/provider/client/openapi_client.rs`
- Modify: `src/provider/client/anthropic_client.rs`

- [ ] **Step 1: 修改 `src/agent/agent.rs`**

在文件顶部添加：

```rust
use crate::log_debug;
```

然后替换以下 5 处 `eprintln!`：

| 原代码 | 新代码 |
|--------|--------|
| `epprintln!("[debug] run_one_turn start \| turn={} \| messages={}", ...)` | `log_debug!("run_one_turn start \| turn={} \| messages={}", ...)` |
| `epprintln!("[debug] LLM tool_use \| id={} \| name={} \| args={}", ...)` | `log_debug!("LLM tool_use \| id={} \| name={} \| args={}", ...)` |
| `epprintln!("[debug] LLM finish_reason={:?}", reason)` | `log_debug!("LLM finish_reason={:?}", reason)` |
| `epprintln!("[debug] assistant message appended \| blocks={}", ...)` | `log_debug!("assistant message appended \| blocks={}", ...)` |
| `epprintln!("[debug] run_one_turn end \| ...")`（3 处） | `log_debug!("run_one_turn end \| ...")`（3 处） |

> 注意去掉原字符串字面量中的 `"[debug] "` 前缀。

- [ ] **Step 2: 修改 `src/tools/mod.rs`**

在 `mod tests` 上方的 `execute_tool_calls` 函数内，找到 3 处 `eprintln!`（`[debug] execute_tool_call ...` 等），替换为 `log_debug!(...)`，并确保顶部有 `use crate::log_debug;`。

- [ ] **Step 3: 修改 `src/provider/client/openapi_client.rs`**

找到 3 处 `eprintln!("[debug] OpenAI assembled tool_call ...")`、`eprintln!("[debug] OpenAI finish_reason=...")`，替换为 `log_debug!(...)`，顶部添加 `use crate::log_debug;`。

- [ ] **Step 4: 修改 `src/provider/client/anthropic_client.rs`**

找到 2 处 `eprintln!("[debug] Anthropic assembled tool_call ...")`、`eprintln!("[debug] Anthropic finish_reason=...")`，替换为 `log_debug!(...)`，顶部添加 `use crate::log_debug;`。

- [ ] **Step 5: 运行全部测试**

Run: `cargo test`
Expected: 26 tests PASS

- [ ] **Step 6: Commit**

```bash
git add src/agent/agent.rs src/tools/mod.rs src/provider/client/openapi_client.rs src/provider/client/anthropic_client.rs
git commit -m "refactor: replace eprintln debug logs with log_debug macro"
```

---

### Task 4: 为 `SessionManager` 新增会话查找与打印方法

**Files:**
- Modify: `src/session/session.rs`
- Modify: `src/session/mod.rs`（如有需要重新导出）

- [ ] **Step 1: 在 `src/session/session.rs` 中新增辅助方法**

在 `SessionManager` impl 块末尾、`session_path` 之后，追加以下两个 `pub` 方法：

```rust
    /// 根据选择器查找单个会话。
    ///   - "last"     -> 最近更新的会话
    ///   - "last-N"   -> 倒数第 N+1 个会话（0-based）
    ///   - 其他       -> 先精确匹配 session_id，再尝试前缀匹配
    pub fn find_session(&self, selector: &str) -> Result<Session> {
        let sessions = self.list_sessions()?;
        if sessions.is_empty() {
            return Err(anyhow::anyhow!("No sessions found"));
        }

        if selector.eq_ignore_ascii_case("last") {
            return self.load_session(&sessions[0].id);
        }

        if selector.to_lowercase().starts_with("last-") {
            let n: usize = selector[5..].parse().unwrap_or(0);
            if n >= sessions.len() {
                return Err(anyhow::anyhow!(
                    "Session index out of range: last-{} (only {} sessions)",
                    n,
                    sessions.len()
                ));
            }
            return self.load_session(&sessions[n].id);
        }

        // 精确匹配
        for m in &sessions {
            if m.id == selector {
                return self.load_session(&m.id);
            }
        }

        // 前缀匹配
        let matches: Vec<_> = sessions.iter().filter(|m| m.id.starts_with(selector)).collect();
        match matches.len() {
            0 => Err(anyhow::anyhow!("No session matches '{}'", selector)),
            1 => self.load_session(&matches[0].id),
            _ => Err(anyhow::anyhow!(
                "Ambiguous session prefix '{}' matches {} sessions",
                selector,
                matches.len()
            )),
        }
    }

    /// 将会话历史打印到 stdout（供 `-s` 使用）。
    pub fn print_session(session: &Session) {
        println!("Session: {} | {} | {} messages", session.id, session.model, session.messages.len());
        println!("---");
        for msg in &session.messages {
            let role_prefix = format!("[{:?}]", msg.role);
            for part in &msg.parts {
                match part {
                    crate::session::message::Part::Text { text } => {
                        println!("{} {}", role_prefix, text);
                    }
                    crate::session::message::Part::ToolUse { id, name, arguments } => {
                        println!("[{:?} -> tool_use: {}] {}", msg.role, name, arguments);
                    }
                    crate::session::message::Part::ToolResult { tool_call_id, content, .. } => {
                        println!("[{:?} -> tool_result: {}] {}", msg.role, tool_call_id, content);
                    }
                    crate::session::message::Part::Image { .. } => {
                        println!("{} [image]", role_prefix);
                    }
                    crate::session::message::Part::Reasoning { thinking, .. } => {
                        println!("{} [reasoning] {}", role_prefix, thinking.chars().take(100).collect::<String>());
                    }
                }
            }
        }
    }
```

- [ ] **Step 2: 添加 `find_session` 的单元测试**

在 `session.rs` 的 `mod tests` 末尾追加：

```rust
    #[test]
    fn test_find_session_last_and_last_n() {
        let (manager, _dir) = temp_manager();
        let s1 = manager.create_session("m1").unwrap();
        let s2 = manager.create_session("m2").unwrap();

        assert_eq!(manager.find_session("last").unwrap().id, s2.id);
        assert_eq!(manager.find_session("last-0").unwrap().id, s2.id);
        assert_eq!(manager.find_session("last-1").unwrap().id, s1.id);

        assert!(manager.find_session("last-99").is_err());
    }

    #[test]
    fn test_find_session_by_id_prefix() {
        let (manager, _dir) = temp_manager();
        let s = manager.create_session("m1").unwrap();
        let prefix = &s.id[..4];

        assert_eq!(manager.find_session(&s.id).unwrap().id, s.id);
        assert_eq!(manager.find_session(prefix).unwrap().id, s.id);
        assert!(manager.find_session("zzzz").is_err());
    }
```

- [ ] **Step 3: 运行测试**

Run: `cargo test session::tests`
Expected: 原有 3 个 + 新增 2 个全部 PASS

- [ ] **Step 4: Commit**

```bash
git add src/session/session.rs
git commit -m "feat: add session find and print helpers for CLI"
```

---

### Task 5: 重构 `src/main.rs` 支持 CLI 分支调度

**Files:**
- Modify: `src/main.rs`
- Modify: `src/mod.rs`（无，只需确保 `cli`/`log` 模块被声明）

- [ ] **Step 1: 在 `src/main.rs` 顶部声明新模块并引入类型**

在 `mod tools;` 下方添加：

```rust
mod cli;
mod log;
```

将 `use provider::Provider;` 替换/补充为：

```rust
use cli::Args;
use clap::Parser;
use log::set_debug;
```

- [ ] **Step 2: 重写 `main` 函数**

将整个 `main` 替换为：

```rust
#[tokio::main]
async fn main() -> Result<()> {
    let args = Args::parse();

    set_debug(args.log_level.eq_ignore_ascii_case("debug"));

    let config_dir = directories::ProjectDirs::from("", "", "shun-code")
        .map(|d| d.config_dir().to_path_buf())
        .unwrap_or_else(|| PathBuf::from(".config/shun-code"));
    let sessions_dir = config_dir.join("sessions");
    let session_manager = SessionManager::new(sessions_dir.clone());

    // -s 优先级最高
    if let Some(session_arg) = args.session {
        if let Some(selector) = session_arg {
            let session = session_manager.find_session(&selector)?;
            SessionManager::print_session(&session);
        } else {
            let sessions = session_manager.list_sessions()?;
            if sessions.is_empty() {
                println!("No sessions found.");
            } else {
                println!("Recent sessions:");
                for (i, s) in sessions.iter().enumerate() {
                    println!(
                        "  [{}] {} | {} | {} messages | {}",
                        i,
                        &s.id[..s.id.len().min(8)],
                        s.project_path,
                        s.message_count,
                        if s.status == SessionStatus::Active { "active" } else { "archived" }
                    );
                }
            }
        }
        return Ok(());
    }

    let provider = Provider::new()?;
    let client = provider.get_client()?;

    // -c 单命令模式
    if let Some(cmd) = args.command {
        let cmd = cmd.trim();
        if !cmd.is_empty() {
            let mut session = session_manager.create_session(provider.model_name()?)?;
            run_single_command(client.as_ref(), &session_manager, &sessions_dir, &mut session, cmd).await?;
        }
        return Ok(());
    }

    // 默认 / -i 交互式模式
    run_interactive(client.as_ref(), &provider, &session_manager, &sessions_dir).await?;
    Ok(())
}
```

- [ ] **Step 3: 新增 `run_single_command` 辅助函数**

在 `main.rs` 中，`run_interactive` 之前插入：

```rust
async fn run_single_command(
    client: &dyn crate::provider::base_client::AIClient,
    session_manager: &SessionManager,
    sessions_dir: &PathBuf,
    session: &mut session::Session,
    query: &str,
) -> Result<()> {
    use crate::session::message::Part;

    let user_msg = Message::new(
        session.id.clone(),
        Role::User,
        vec![Part::Text { text: query.to_string() }],
    );
    session.messages.push(user_msg.clone());
    let _ = session_manager.append_message(&session.id, &user_msg);

    let mut state = LoopState::new(session.messages.clone());
    agent_loop(client, &mut state).await?;
    session.messages = state.messages;

    if let Err(e) = tokio::task::spawn_blocking({
        let sm = SessionManager::new(sessions_dir.clone());
        let s = session.clone();
        move || sm.save_session(&s)
    }).await?
    {
        eprintln!("Warning: failed to save session: {}", e);
    }

    if let Some(last_msg) = session.messages.last() {
        if last_msg.role == Role::Assistant {
            let text = provider::extract_text(&last_msg.parts);
            if !text.is_empty() {
                println!("{}", text);
            }
        }
    }
    Ok(())
}
```

> **修正**：`run_single_command` 签名里不需要 `&provider`，只需要 `model_name` 已经在创建 session 时用过了。

- [ ] **Step 4: 提取原有的 REPL 逻辑为 `run_interactive`**

将原来 `main` 中的 loop 提取为：

```rust
async fn run_interactive(
    client: &dyn crate::provider::base_client::AIClient,
    provider: &Provider,
    session_manager: &SessionManager,
    sessions_dir: &PathBuf,
) -> Result<()> {
    let mut session = choose_or_create_session(session_manager, provider.model_name()?).await?;
    let prompt_prefix = format!("{} >> ", &session.id[..session.id.len().min(8)]);
    let mut editor = DefaultEditor::new()?;

    loop {
        let readline = editor.readline(prompt_prefix.cyan().to_string().as_str());
        match readline {
            Ok(line) => {
                let query = line.trim();
                if query.is_empty() || ["q", "exit"].contains(&query.to_lowercase().as_str()) {
                    break;
                }
                editor.add_history_entry(query)?;

                let user_msg = Message::new(
                    session.id.clone(),
                    Role::User,
                    vec![session::message::Part::Text { text: query.to_string() }],
                );
                session.messages.push(user_msg.clone());
                if let Err(e) = session_manager.append_message(&session.id, &user_msg) {
                    eprintln!("Warning: failed to persist user message: {}", e);
                }

                let mut state = LoopState::new(session.messages.clone());
                agent_loop(client, &mut state).await?;
                session.messages = state.messages;

                if let Err(e) = tokio::task::spawn_blocking({
                    let sm = SessionManager::new(sessions_dir.clone());
                    let s = session.clone();
                    move || sm.save_session(&s)
                }).await?
                {
                    eprintln!("Warning: failed to save session: {}", e);
                }

                if let Some(last_msg) = session.messages.last() {
                    if last_msg.role == Role::Assistant {
                        let text = provider::extract_text(&last_msg.parts);
                        if !text.is_empty() {
                            println!("{}", text);
                        }
                    }
                    println!();
                }
            }
            Err(rustyline::error::ReadlineError::Interrupted)
            | Err(rustyline::error::ReadlineError::Eof) => break,
            Err(err) => {
                eprintln!("Error: {:?}", err);
                break;
            }
        }
    }
    Ok(())
}
```

- [ ] **Step 5: 编译检查**

Run: `cargo check`
Expected: 无错误

- [ ] **Step 6: 运行全部测试**

Run: `cargo test`
Expected: 全部 PASS

- [ ] **Step 7: Commit**

```bash
git add src/main.rs
git commit -m "feat: wire CLI args into main with -s / -c / interactive branches"
```

---

### Task 6: 手动 CLI 行为验证

**Files:** 无代码修改，纯验证

- [ ] **Step 1: 验证 `--version` 和 `--help`**

Run:
```bash
cargo run -- --version
cargo run -- --help
```

Expected:
- `--version` 输出 `shun-code 0.1.0`
- `--help` 输出所有选项的说明

- [ ] **Step 2: 验证 `-s` 各模式**

Run:
```bash
cargo run -- -s
cargo run -- -s last
cargo run -- -s last-0
```

Expected: 根据已有 session 文件分别输出列表、最近会话历史。

- [ ] **Step 3: 验证 `-c` 单命令模式**

Run:
```bash
cargo run -- -c "hello"
```

Expected: 执行单轮对话后退出。

- [ ] **Step 4: 验证 `-l debug` 生效**

Run:
```bash
cargo run -- -l debug -c "hello"
```

Expected: stderr 中出现 `[debug] ...` 日志。

- [ ] **Step 5: Commit（如测试通过）**

```bash
git commit --allow-empty -m "test: verify CLI behaviors manually"
```

---

## Self-Review Checklist

1. **Spec coverage:**
   - `-v/--version`、`-h/--help`：由 clap 自动处理 ✅
   - `-l/--log=debug`：Task 1 + Task 2 实现 ✅
   - `-i/--interactive`：默认分支 ✅
   - `-s/--session`：Task 4 + Task 5 实现 ✅
   - `-c/--command`：Task 5 实现 ✅

2. **Placeholder scan:** 无 TBD/TODO/"implement later" ✅

3. **Type一致性:** `log_debug!` 宏、`Args` 结构体、`find_session` 签名在设计和计划中使用一致 ✅
